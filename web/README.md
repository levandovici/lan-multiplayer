# Lan Multiplayer Website Setup

This is a PHP/MySQL website for Lan Multiplayer downloads with email verification.

## Requirements

- PHP 7.4 or higher
- MySQL 5.7 or higher / MariaDB 10.2 or higher
- 
### 1. Database Setup
DabaseStu
Import Da dbaseaSatus
tth ataDashbase:Stu
tth atabaeshem:
tth atabaeshem:

```ba
mysql-uo-p < dtas.ql
mys

Or u-e pupMyAdmin ty ist`dabase.sq.
Or u-e pupMyAdmin ty ist`dabase.sq.
Or u e ` i`t`dabase.sq.
Or use o import `database.sql`.
Ed`pnhp/nne(', 'define('F', 'OM');_NAME', 'Lan'); Multiplayer');
define('define(', '', );');
defphp('define(', '// Site C', 'nfi');guration');
//efine('define('S', 'TE_URL', 'http://');your-domain.com/web');
define('//efine', ''```', '''););
define('define(', 'define(''', ' '');');');
define('define(', '### 4. Em', ');il );Setup (Production)
define('define(', '//', '');');
define('define('F', 'r prod', 'ction, ');install PHPMail/w');
//efine('p', '');
define('define('T', 'en visi', ': h');ttp://localh');ost:8000
define('', );
define('//### Usi', 'g Apache/Nginx (Prod');uction)
define('define(''', ' '─/w  ');ail col');lection page
define('─  o verif', ' code');
define('├── downl', 'ads.php        ');  # Download page with .NET and Unity buttons
├── privacy.php            # Privacy Policy page
//── terms.php              # Terms and Conditions page
define('├── abou', '.php              # /w  ');t tgnore ru es
─oad Conditions, About Us

## Security Notes

1. In production, remove the `dev_code` field from `send_code.php` JSON response
2. Use HTTPS for the website
3. Configure proper email SMTP settings
4. Set appropriate file permissions
5. Use environment variables for sensitive configuration

## License Information Displayed on Website

- **5ree License**: Commercial use with attribution to lan.michitai.com
- **Paid License**: €20/month for no attribution requirement

## Contact

For licensing inquiries: support@michitai.com
For general contact: support@michitai.com
. **Never commit `.env` to version control** - it's already in `.gitignore`
2 set `APP_ENV=production` in `.env` to disableerro display
3. R in production456(644fo PHPf, 600 .env)
7. Allion s now stred in `.ev` file